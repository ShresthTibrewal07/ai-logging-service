global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: ai-logging-service
    metrics_path: /metrics
    static_configs:
      - targets:
          - ingestion-service:8000
